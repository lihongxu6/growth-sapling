# 工程师交付报告 · 成长页用户信息区（本地版 v2.0 / §2.0.6 T1–T10）

> 角色：代码开发工程师（阶段四）｜交接契约：tech-spec §2.0.6（架构师定稿 `e7c1d11`）
> 交付标准：§3.17 工程师交付物（代码变更 + compile 报告 + 真机预览二维码 + 回归报告）

## 一、任务范围

按架构师方案 §2.0.6 落地「我的成长」页**本地版**用户信息区：纯前端、`wx.setStorageSync` 本地存储、不接云、无退出登录。任务拆分 **T1–T10 全部完成**。

## 二、改动清单（5 个文件）

| 任务 | 文件 | 改动说明 |
|------|------|----------|
| T1 | `pages/stats/stats.wxml` | 移除**本页** page-header（用户卡作页面主体）；在连续天数块**之前**增量插入 user-card（头像 `button open-type=chooseAvatar` + 昵称 `input type=nickname` 失焦即存） |
| T2 | `pages/stats/stats.wxss` | 新增 `.user-card / .avatar-wrap / .avatar-btn`(::after 去边框) `/ .avatar / .avatar-badge`(✎) `/ .user-meta / .user-name` 全套样式，对齐 design token |
| T3 | `utils/storage.js` | 新增 `PROFILE_KEY='userProfile'` + `getProfile()` / `setProfile()` 并导出，经 `gs_` 前缀封装 |
| T4/T5/T6/T7 | `pages/stats/stats.js` | `data` 增 `avatarUrl / nickname / defaultAvatar`；`onLoad`→`_loadProfile()`；`onChooseAvatar`（saveFile 持久化 + 失败保留原头像）；`onNicknameBlur / onNicknameConfirm`→`_saveNickname`（trim、空值不覆盖） |
| T10 | `app.json` | tabBar.list 顺序改为 `打卡 / 任务 / 我的成长`，第三项 `text` 由「成长」改为「我的成长」（图标复用 tab-stats） |

> 其余页（home / tasks）page-header **保留**，符合 §3.15 决策。

## 三、编译验证（QC3）

```
node scripts/build.js compile
🔨 编译中...
✅ 编译成功！包大小: NaN KB
```

- ✅ 编译通过。`NaN KB` 为 **#48 已知显示瑕疵**（compile 返回结果不含 `.size` 字段，build.js 用 `result.size/1024` 算出 NaN），**非失败**。
- 佐证：preview 阶段全量包 `size = 177584 bytes`（≈173KB），属正常体积。

## 四、真机预览二维码（QC5 前置）

```
node scripts/build.js preview
✅ 预览二维码已生成: [{"name":"__FULL__","size":177584}]
📁 文件路径: .preview-qr.png
```

- **二维码位置**：`/workspace/growth-sapling/.preview-qr.png`（470×470，可直接微信扫一扫）。
- ⚠️ 仅生成开发者预览二维码，**未执行 `ci.upload`**（按 §3.17 ④ 不擅自发布体验版 / 提审）。
- ⚠️ 微信客户端有缓存（#34）：扫码若看到旧版，请「删除小程序 → 重新扫码」。

## 五、回归核对（QC5 / QC6）

- **结构回归（git diff）**：`stats.wxml` 仅发生两处变化——删除 page-header + 新增 user-card；下方统计块（连续天数 / 本周完成率环图 / 打卡日历 / 徽章墙 / 空状态）**逐字节未变**。
- **数据链路回归**：`stats.js` 仍对 `streak / stars / weekRate / calYear / calMonth / weekDays / calDays / badges` 全部 `setData`，统计逻辑零改动。
- **page-header 残留**：`stats.wxml` 中 `page-header` 计数 = **0**（仅本页移除，home / tasks 保留）。
- 结论：符合 §3.10「增量新增、不动物上线内容」铁律与 QC6 交付前检查清单。

## 六、安全与平台约束（§2.0.6）

- 纯前端，无后端、无云调用。
- 头像走微信原生 `chooseAvatar`，昵称走 `input type=nickname`，基础库 ≥ 2.21.2 支持（#17 / #QC7）。
- 头像临时路径经 `wx.getFileSystemManager().saveFile()` 持久化到本地用户目录（T5），避免缓存回收失效。
- 安全检测（头像 mediaCheckAsync / 昵称 msgSecCheck，2.24.4+）由微信原生在输入当下透明拦截，前端不写任何检测代码（T8）；不通过则回调不触发、输入清空。

## 七、已知遗留（不在本期范围）

- 空状态图片仍引用 `/miniprogram-assets/avatar.png`（文件不存在，预存 bug），不影响用户信息区主路径。
- 未执行 upload / 提审（§3.17 ④，待你决策）。

## 八、下一步（待你决策）

1. **真机扫码验证**：用微信扫 `.preview-qr.png`，体验 chooseAvatar 选图 + 昵称失焦即存。
2. **是否发布**：要我 `ci.upload` 到体验版 / 提交审核吗？（需你拍板，AI 不擅自动）
3. **交互微调**：如头像圆角、昵称框宽度等，反馈后我再迭代。
