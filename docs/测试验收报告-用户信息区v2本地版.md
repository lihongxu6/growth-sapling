# 测试验收报告 · 成长页用户信息区（本地版 v2.0 / §2.0.6 T1–T10）

> 角色：测试工程师（阶段五）｜前置：MEMORY §3.17「测试前置」规则（交付真机二维码前先排沙箱可测问题）
> 方法：test-automation 用例矩阵（正常×1 + 边界×2 + 异常×1）+ Node + mock `wx` 可执行脚本 `scripts/test_userinfo.js`

## 一、概要

| 项目 | 结果 |
|------|------|
| 沙箱可执行逻辑测试 | **12 / 12 ✅（通过率 100%）** |
| 静态 / 编译核查（QC3/QC4/QC2/QC6） | ✅ 全部通过 |
| 原生组件交互（chooseAvatar / input type=nickname） | ⏭️ **待真机确认**（沙箱 / DevTools 无法模拟，#22/#34/#QC7） |
| 总结论 | 沙箱可测工程问题 **已清零**，可交你真机扫码做最后一层确认 |

## 二、沙箱已验 · 用例矩阵（12 项，全部 PASS）

| ID | 分类 | 功能点 | 前置条件 | 操作步骤 | 期望结果 | 对应 | 优先级 |
|----|------|--------|----------|----------|------------|------|--------|
| T-U01 | 正常 | setProfile/getProfile 往返 | 空存储 | setProfile({avatarUrl,nickname}) | getProfile 深等于写入值 | §2.0.6 T3 | P0 |
| T-U02 | 边界 | 未知键读取 | 空存储 | getProfile() | 返回 null（非报错） | T3 | P1 |
| T-U03 | 边界 | 空字符串存储 | 存 `''` | getProfile() | 返回 null（视为无数据） | T3 | P1 |
| T-U04 | 正常 | onLoad 首次无 profile | 空存储 | onLoad() | data.avatarUrl=''、nickname='' | T4 | P0 |
| T-U05 | 正常 | onLoad 回填已有 profile | 存 {old.png,果果} | onLoad() | data 回填 old.png / 果果 | T4 | P0 |
| T-U06 | 正常 | chooseAvatar 成功持久化 | fsMode=success | onChooseAvatar(tmp) | profile.avatarUrl=local_1.png + 视图更新 | T5 | P0 |
| T-U07 | 异常 | chooseAvatar 失败保留原图 | 存 old.png，fsMode=fail，先 onLoad | onChooseAvatar(tmp) | profile 与视图均保持 old.png（不写坏路径） | T7 | P0 |
| T-U12 | 边界 | chooseAvatar 临时路径为空 | 存 old.png | onChooseAvatar('') | 直接 return，profile 不变 | T5 | P1 |
| T-U08 | 正常 | 昵称 blur 正常保存 | 空存储 | onNicknameBlur('小明') | profile.nickname='小明' + 视图更新 | T6 | P0 |
| T-U09 | 边界 | 昵称 blur 空值不覆盖 | 存 {old,果果} | onNicknameBlur('') | 旧昵称保留 | T6 | P1 |
| T-U10 | 边界 | 昵称含首尾空格 | 空存储 | onNicknameBlur(' 小明 ') | trim → '小明' | T6 | P1 |
| T-U11 | 正常 | 昵称 confirm 同 blur 路径 | 空存储 | onNicknameConfirm('乐乐') | profile.nickname='乐乐' | T6 | P0 |

**执行命令**：`node scripts/test_userinfo.js` → 通过 12 / 失败 0 / 跳过 0。

## 三、静态 / 编译核查（QC 体系）

| 检查 | 结果 | 说明 |
|------|------|------|
| QC3 编译 | ✅ | `miniprogram-ci compile` 通过（NaN KB 为 #48 显示瑕疵，preview 实测全量包 ≈173KB） |
| QC2 三件套 | ✅ | stats 具 wxml/wxss/js；`.json` 为非 custom 页**可选**，项目规范 home/tasks/stats 均不配，splash 因 custom 才配，编译已通过 → **非缺陷** |
| QC4 数据绑定 | ✅ | WXML 绑定变量 `avatarUrl/nickname/streak/stars/weekRate/calYear/calMonth/weekDays/calDays/badges` 全部在 stats.js 的 data 或 setData 中声明/赋值 |
| QC5/QC6 回归 | ✅ | `git diff` 证明 stats.wxml 仅两处结构变化（删 page-header + 增 user-card）；下方统计块（连续天数/完成率/日历/徽章墙/空状态）**逐字节未变**；`page-header` 残留计数 = 0（仅本页移除，home/tasks 保留，§3.15）；stats.js 统计逻辑零改动 |

## 四、⏭️ 待你真机确认（微信原生组件，沙箱测不了）

> `chooseAvatar` 与 `input type=nickname` 是**微信原生组件**，DevTools 模拟器不还原真机行为（#22/#34/#QC7 反复踩坑）。以下必须由你**微信扫 `.preview-qr.png`** 在真机验证——这是平台硬约束，不是测试遗漏。

| ID | 分类 | 验证点 | 期望结果 |
|----|------|--------|------------|
| T-R01 | 正常 | 点头像区域 | 微信原生 chooseAvatar 浮层弹出（默认头像 / 相册 / 拍照） |
| T-R02 | 正常 | 选图后 | 头像即时更新；杀掉小程序重进仍在（saveFile 持久化生效） |
| T-R03 | 正常 | 点昵称 | 调起微信昵称输入键盘，可一键填充微信昵称（input type=nickname） |
| T-R04 | 正常 | 昵称失焦 / 点确认 | 即时存本地 + 视图更新；空值不覆盖（T-U09 沙箱已证逻辑） |
| T-R05 | 边界 | 基础库 < 2.21.2 设备 | 原生组件不可用；我们选**方案 A 不做降级**（已知决策 §3.15），该情况属预期外，不在此版处理 |
| T-R06 | 视觉 | user-card 真机渲染 | 头像圆 / ✎ 角标位置 / 昵称框宽度无错位（沙箱只能验编译不能验渲染，#22/#34） |

## 五、回归风险

- **低风险**：统计页数据逻辑（calcStreak / calcStars / 日历 / 徽章）完全未触碰，L-list（MEMORY §5：L8.1/L8.4/L8.5）逐项不受影响。
- **唯一需你把关**：原生组件真机表现（T-R01~T-R06）。若扫码发现交互异常（如浮层不弹、昵称不回填），反馈后我按 code-change-workflow 两步确认再改。
- **已知遗留（非本轮问题）**：空状态图误引 `/miniprogram-assets/avatar.png`（不存在，预存 bug），不影响用户信息区主路径。

## 六、结论与下一步

1. **沙箱自测 12/12 通过 + 静态/回归全绿** → 工程侧可测问题已清零，二维码可放心交你扫。
2. **请你微信扫 `.preview-qr.png`**，按第四节 T-R01~T-R06 完成真机确认（⚠️ 微信有缓存，看到旧版请「删除小程序→重新扫码」）。
3. 真机确认 OK 后，是否 `ci.upload` 体验版 / 提审由你拍板（§3.17 ④，AI 不擅自动）。
